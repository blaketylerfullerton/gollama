# Model weights — downloaded via `hf download`, not versioned
checkpoints/

# Compiled binary (go build -o GoLlama)
/GoLlama

# goreleaser output (goreleaser release --snapshot --clean)
/dist

# Go build/test artifacts
*.test
*.out

# Trace files produced by `go run . -trace ...`
*.jsonl

python/